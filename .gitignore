# Build output
/lawnmower
/bin/

# Your real config (copy config.example.yaml to this and fill it in)
config.yaml

# Your real secrets (copy .env.example to this and fill it in)
.env
.env.*
!.env.example

# Editor/OS cruft
.DS_Store

# Local run output (e.g. from cron)
log.txt
