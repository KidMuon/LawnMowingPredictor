# Copy this file to .env and fill in your real values. .env is gitignored
# so these never end up committed. Neither of these is ever read from
# config.yaml - only from the environment (directly, or via this file).

OPENWEATHERMAP_API_KEY=
TODOIST_API_TOKEN=
